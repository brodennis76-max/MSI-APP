// React Native Firebase configuration
// This file is used by @react-native-firebase/app

// For React Native Firebase, the configuration is typically handled
// through the firebase.json file and platform-specific config files

export default {
  // Configuration is handled by firebase.json and platform configs
  // No need to export config object for React Native Firebase
};









